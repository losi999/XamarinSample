<Page
    x:Class="XamarinSample.Windows81.View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XamarinSample.Windows81"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Main, Source={StaticResource Locator}}">

    <StackPanel
        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button
            Command="{Binding CommandDialogsPage}"
            Content="Go to dialogs page" />
        <Button
            Command="{Binding CommandSettingsPage}"
            Content="Go to settings page" />
        <Button
            Command="{Binding CommandPersonsPage}"
            Content="Go to persons page" />
    </StackPanel>
</Page>
